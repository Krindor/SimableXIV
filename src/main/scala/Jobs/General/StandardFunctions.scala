package Jobs.General

import models.SimModel

class StandardFunctions {
  def attackStart(simModel: SimModel): SimModel ={
    simModel
  }

  def damageOverTime(simModel: SimModel): SimModel ={
    simModel
  }

  def attackEnd(simModel: SimModel): SimModel ={
    simModel
  }

}
